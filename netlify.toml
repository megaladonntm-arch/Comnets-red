[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  VITE_API_URL = "https://comnets-red-production.up.railway.app"
  VITE_WS_URL = "wss://comnets-red-production.up.railway.app"
